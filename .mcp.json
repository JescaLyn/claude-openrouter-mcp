{
  "mcpServers": {
    "openrouter": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "OPENROUTER_API_KEY": "${user_config.openrouter_api_key}"
      }
    }
  }
}
